describe('PropertiesRepository', () => {
  test.skip(`does not needs tests while none method is overridden, 
because the repository is already tested on the base class`, () => {
    expect(true).toBe(true);
  });
});
